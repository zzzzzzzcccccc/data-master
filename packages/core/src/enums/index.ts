export enum ASYNC_STATUS {
  idle = 'idle',
  pending = 'pending',
  fulfilled = 'fulfilled',
  rejected = 'rejected',
}

export enum THEME_MODE {
  dark = 'dark',
  light = 'light',
  system = 'system',
}

export enum SIZE {
  small = 'small',
  middle = 'middle',
  large = 'large',
}
